@model ProjectVideo.Web.Models.Proposals.ProposalsIndexViewModel

<div class="card mt-3">
    <div class="card-header">
        <div class="row">
            <div class="col">
                <h1 class="card-title h3">Project Proposal</h1>
            </div>
            <div class="col text-end align-con">
                <a class="btn btn-outline-secondary" asp-controller="Proposals" asp-action="Form">
                    <i class="bi bi-ui-checks"></i>
                    Public Form
                </a>
            </div>
        </div>
    </div>
    <div class="card-body">
        <table class="table">
            <thead>
                <tr>
                    <th>Date Submitted</th>
                    <th>Organization Name</th>
                    <th>Project Title</th>
                    <th>Contact Name</th>
                    <th>Target Audience</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var proposal in Model.Data.Proposals)
                {
                    <tr>
                        <td>@proposal.DateSubmitted.ToShortDateString()</td>
                        <td>@proposal.OrganizationName</td>
                        <td>@proposal.ProjectTitle</td>
                        <td>@proposal.ContactName</td>
                        <td>@proposal.TargetAudience</td>
                        <td>
                            <button class="btn btn-primary">
                                <i class="bi bi-pen-fill"></i>
                                Edit
                            </button>
                        </td>
                    </tr>
                }
                @if (Model.Data.Proposals.Count == 0)
                {
                    <tr>
                        <td colspan="6">
                            <div class="alert alert-info">
                                <i class="bi bi-info-circle-fill"></i>
                                No Proposals Found.
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>